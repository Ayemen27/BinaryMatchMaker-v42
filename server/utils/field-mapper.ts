/**
 * تم تعديل هذا الملف كجزء من خطة إصلاح التطابق بين أسماء الحقول
 * 
 * المشكلة السابقة:
 * - كان الكود يقوم بتحويل أسماء الحقول بين تنسيقين مختلفين: snake_case (قاعدة البيانات) و camelCase (التطبيق)
 * - هذا التحويل كان سببًا في تعقيد الكود وصعوبة تتبع أسماء الحقول
 * 
 * الحل الجديد:
 * - استخدام نفس أسماء الحقول في كل من قاعدة البيانات والتطبيق
 * - عدم تحويل الأسماء تلقائيًا بين التنسيقين
 * - ضمان التطابق المباشر بين أسماء الحقول في كل طبقات التطبيق
 */

/**
 * وظيفة محدثة لتمرير البيانات كما هي دون تحويل أسماء الحقول
 * 
 * استبدلت وظيفة تحويل أسماء الحقول من snake_case إلى camelCase
 */
export function mapDatabaseToApp<T>(data: any): T {
  // إعادة البيانات كما هي بدون تحويل
  return data as T;
}

/**
 * وظيفة محدثة لتمرير البيانات كما هي دون تحويل أسماء الحقول
 * 
 * استبدلت وظيفة تحويل أسماء الحقول من camelCase إلى snake_case
 */
export function mapAppToDatabase<T>(data: any): T {
  // إعادة البيانات كما هي بدون تحويل
  return data as T;
}

/**
 * وظيفة محدثة لتمرير البيانات كما هي
 * 
 * ملاحظة: تم الحفاظ على هذه الوظيفة للتوافق مع الكود القديم
 */
export function mapDatabaseToAppWithOriginal<T>(data: any): T {
  // إعادة البيانات كما هي بدون تحويل
  return data as T;
}